<% title "#{@show.full_name} | Review" %>
<h2><%= link_to @show.full_name, [@user,@show] %> | Review</h2>
<br/>

<% form_for([current_user, @show, @review]) do |f| %>
  <%= f.error_messages %>

  <p>
    Review<br />
    <%= f.text_area :entry %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Delete', [@user, @show, @review], :confirm => 'Are you sure?', :method => :delete %> |
<%= link_to 'Back to show', user_show_path(@user, @show) %>
