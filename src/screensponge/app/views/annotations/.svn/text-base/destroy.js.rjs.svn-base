page[:annotations].replace_html :partial => 'annotations/annotations', 
                                :locals  => {:annotations => @show.annotations,
                                             :show => @show}
